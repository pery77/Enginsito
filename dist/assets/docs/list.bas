'LIST         -> ' Creates a list.
'A list begins from zero as well as how array does.

let l = list(1, 1, "x", 5, 3, 2, 9)
print len(l) 'print 7 because l have 7 elements

print l(2) 'print x because is de third index, yes 0 is the first

'create a list and print it
let l2 = list(80 to 50)
for n in l2
	print n
next